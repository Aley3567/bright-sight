# CLAUDE.md — bright-sight 仓库约定

这份文件约束的是**在这个仓库里写代码**这件事。它不重复 README 的设计叙事，只写下
「什么能进仓库、什么不能」以及为什么。

这个项目会开源，也会被别人 clone 到别人的机器上跑。所以下面每一条的判据都是同一句话：

> 换一台机器、换一个人、换一个 Chrome profile、换一个语言环境，这段代码还成立吗？

---

## 一、零硬编码：什么算「本地死数据」

**禁止把只在某一台机器上成立的事实写进源码。** 具体包括：

| 类别 | 反例 | 正确做法 |
|---|---|---|
| 家目录路径 | `/Users/某人/Library/...` | `homedir()` 拼，见 `src/chrome.ts` |
| Chrome profile 目录名 | `"Profile 2"` | 运行时探测，`src/chrome.ts` |
| Chrome profile 显示名、账号、邮箱 | `"工作账号"` | 只在提问那一瞬读一次，不落盘不进留痕 |
| 笔记文件夹名、账号名 | `folder "Notes"` | `default folder of default account` |
| 代理地址、端口 | `http://127.0.0.1:<某个端口>` | 不写。本项目不该知道有代理这回事 |
| 机器名、用户名 | 任何形式 | 不写 |
| 真实 URL、真实笔记 id | 从自己机器上抄来的 | 测试里用 `example.com` 与随机串 |
| API key、token | 任何形式 | 走环境变量，见下 |

**判断标准不是「这条信息敏感吗」，而是「别人的机器上这条信息还对吗」。**
`Profile 2` 不敏感，但它在别人机器上是错的，所以同样禁止。

### 允许出现在源码里的固定字符串

- **平台约定**：`~/Library/Application Support/Google/Chrome` 这一段后缀（拼在 `homedir()` 后面）、
  `/System/Library/ScriptingDefinitions/CocoaStandard.sdef`。它们对所有 macOS 都一样。
- **应用名**：`"Notes"`、`"Google Chrome"`——它们是 `tell application "…"` 的一部分，是协议不是数据。
  集中在 `src/config.ts` 的 `ALLOWED_APPS` 与 `CHROME_APP`，不要散落成字面量。
- **搜索引擎 URL 模板**：集中在 `src/config.ts`，并且那个文件头部解释了它为什么算配置数据而不是适配代码。

### 因人而异的东西放哪

`~/.bright-sight/config.json`（`src/settings.ts`）。它在家目录，不在仓库里——仓库会被 clone，家目录不会。
新增一项「每台机器不一样」的配置时，加到 `Settings` 类型里，不要新开一个文件，也不要读环境变量当配置
（环境变量只用于**行为开关**，见下）。

### 环境变量

只用于开关，不用于存数据：

| 变量 | 用途 | 默认 |
|---|---|---|
| `TYPESAFE_API_KEY` | 模型凭证 | 无，`run` 必需 |
| `BRIGHTSIGHT_ENGINE` | 搜索引擎 | `google` |
| `BRIGHTSIGHT_JOURNAL` | 留痕脱敏，`full` 才落原文 | 脱敏 |
| `BRIGHTSIGHT_LIVE` | 设为 `1` 才跑有真实副作用的测试 | 不跑 |

**排查凭证问题时不要读 key 的值**，只看变量名和错误信息。

---

## 二、隐私：默认落在安全的那一边

这个项目会读用户说的每一句话、看到的每一个窗口标题、打开的每一个网址。
开源之后，每个跑它的人都会在自己磁盘上攒下这些东西。

**规则：任何新增的落盘、上报、日志，默认必须是脱敏的。** 想要原文，让人显式打开开关。

- 留痕走 `src/redact.ts`。新增 `phase` 时**同时**去 `redactEvent` 加一条规则；
  忘了加会落到 `default` 分支被全量指纹化——过度脱敏是可接受的失败，静默泄露不是。
- 指纹是 HMAC 加盐，盐每台机器一份、存在家目录。不要改成裸哈希：
  常见网站和窗口标题的候选集很小，裸哈希可以用彩虹表还原。
- 新增「给人看」的输出（CLI 打印、报错信息）可以含原文，但**不要顺手把它也写进留痕**。
- `.gitignore` 里那几行运行时数据不是样板，改之前先去 `~/.bright-sight/` 看一眼里面有什么。
- **新增落盘的文件要显式给权限**（目录 `0o700`、文件 `0o600`），不要听凭 umask。
  作者机器上 umask 恰好是 077 看不出问题，别人机器上默认是 022，同机的其他用户就读得到了。
  留痕与 `config.json`（里面有指纹盐）都已经这么做。

---

## 三、安全边界的形状

三件事必须保持原样，改动时先想清楚代价：

1. **`src/osa.ts` 是唯一 spawn `osascript` 的地方。** 不变式是「标识符可以拼进脚本文本，值永远不行」。
   多一处 spawn，这条不变式就要多证明一次。
2. **`src/policy.ts` 零 IO、纯函数。** 它是安全边界，而能被穷举测试的前提是它不依赖外部世界。
   需要外部信息（比如 profile 探测结果）就作为参数传进去，不要在里面读盘。
3. **脚本模板里没有插值。** 测试会逐字断言每个 `src` 不含 `${`。所有动态值走 argv。

### 缺省值一律 fail-closed

新增安全判据时，**「调用方没传」必须等于「拦住」**，不能等于「放行」。
`PolicyInput.profile` 是现成的例子：省略它等于 `undetectable`，于是 Chrome 动作一律走 confirm。
省略等于放行的话，忘记接线就成了静默的安全漏洞。

---

## 四、AppleScript 的四条硬约束

踩过的坑，别再踩：

1. **tell 块里不能对应用对象的属性做二次运算**（`length of`、`contains`）——会被当成远程引用，报 -1700。
   先 `set x to <属性>` 落成本地变量再运算。
2. **`container of` 不能链式写**（`name of (container of n)`），要分两步。
3. **保留字不能当变量名，而这张表没有文档。** 已踩中：`after`、`at`（`make new … at …` 的参数名）、
   `st`（序数后缀，如 1st）。踩中只会在**真跑的那一刻**报 -2741。
   门禁已经有了：`test/compile.test.ts` 用 `osacompile` 把每个模板编译一遍，不执行、不发 Apple Event，
   所以无条件跑在 `npm test` 里。**新增模板时不要绕过它。**
4. **一律按只读 id 定位，不按位置定位。** 同一类缺陷已经出现四次：

   | # | 场景 | 以为 | 实际 |
   |---|---|---|---|
   | 1 | 两个同名 `Notes` 文件夹 | `folder "Notes"` 唯一 | 静默指向第一个 |
   | 2 | 同名笔记回读 | 按 name 找得到 | 同名会撞 |
   | 3 | Chrome `window 1` | 是我们开的窗口 | 是最前面的窗口，profile 一换就不是了 |
   | 4 | 测试的清理代码 | 关的是自己开的标签页 | 同 3，可能关掉用户正在用的页面 |

   解法统一：在**创建的那一瞬间**抓住 id（那是唯一能确信「这个对象就是我刚造的」的时刻），
   此后全程按 id 重新查找。

---

## 五、测试

- `npm run check` = `tsc --noEmit` + `node --test`。提交前跑。
- **有真实副作用的测试默认 skip**，`BRIGHTSIGHT_LIVE=1` 才跑（`npm run test:live`）。
  别人跑 `npm test` 不该被要求先去系统设置里授自动化权限，也不该在别人的备忘录里凭空多出几条笔记。
- live 测试**必须自己收拾**：按 id 删、按 id 关，只碰自己造出来的东西。
  绝不 `close every window`、绝不「删到报错为止」（`delete` 是异步的，重复删除不报错）。
- **新增一条判据时，要补一个「新旧分支同时可能命中」的测试。**
  只测新逻辑的顺风路径，会让「判据装错了位置」「把不该管的也管了」这类缺陷全绿通过。
  `test/policy.test.ts` 末尾那组 profile 闸用例是现成的样子：拦得住、不越界、优先级正确，三条都要。
- 任何「0 命中 / 没找到 / 很干净」的结论**必须配阳性对照**。
  本机 `grep` 是 ugrep 的包装，zsh 又不做 word splitting，很容易得到一个假的干净结果。
  `test/compile.test.ts` 末尾那条对照测试是同一个道理：不证明工具在工作，全绿就什么都不说明。

---

## 六、代码风格

- 注释用中文，与现有代码一致。
- **注释写「为什么」，不写「是什么」。** 这个仓库里几乎每一段非显然的代码都附着一条实测结论或一个权衡，
  照着那个密度写。复述代码做了什么的注释不要写。
- TypeScript 配了 `erasableSyntaxOnly` + `verbatimModuleSyntax`：不能用 enum 和构造函数参数属性，
  导入一律带 `.ts` 后缀，类型导入写 `import type`。
- 零运行时依赖是刻意的（只有 `@typesafe-ai/sdk` 一个）。加依赖之前先问：省下的几十行值不值一处对外耦合。
- 新逻辑优先从已有逻辑延伸，而不是并列新增一个分支。必须并列时按真实依赖顺序放置
  （精确 → 特化 → 兜底）。
